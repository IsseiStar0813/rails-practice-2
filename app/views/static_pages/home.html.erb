<% if session[:user_id] %>

<%= form_with(model: @micropost, local: true) do |f| %>
  <%= f.text_area :content, placeholder: "Compose new micropost..." %>
<%= f.submit "Post"%>
<% end %>

<% else %>
<h1>Welcome to Rails Practice!</h1>
<p>Rails 学習中です</p>
<h2><%= link_to "Sign up now!", signup_path %></h2>
<% end %>